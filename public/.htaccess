Options -MultiViews
RewriteEngine On

# Se o arquivo ou pasta solicitada não existir fisicamente...
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# ...redirecione tudo para index.php
RewriteRule ^ index.php [QSA,L]